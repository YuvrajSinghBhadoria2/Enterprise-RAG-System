app:
  title: "Enterprise RAG Search"
  host: "0.0.0.0"
  port: 8080

retrieval:
  top_k_retrieval: 20
  top_k_rerank: 5
  weights:
    bm25: 0.3
    dense: 0.7

embeddings:
  model_name: "BAAI/bge-m3"
  device: "cpu" # or cuda

reranker:
  model_name: "cross-encoder/ms-marco-MiniLM-L-6-v2"

ingestion:
  chunk_size: 512
  chunk_overlap: 50
